This is an R package to fit SDMs in R using a point process formulation. It uses INLA to do the fitting, but this needs a bit of fiddling around to get the data into the right format.


To Do

GetNearestCovariate() needs to be re-written
A test suite should be developed.
