This is an R package to fit SDMs in R using a point process formulation. It uses INLA to do the fitting

To Do

- A test suite should be developed for data.
- Add functionality to FitModel to specify the model formula
