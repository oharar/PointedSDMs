This is an R package to fit SDMs in R using a point process formulation. It uses INLA to do the fitting

To Do

- A test suite should be developed.
- Add a vignette, with hte solitary tinomou data
- Check it works with anything other than the current data
